# 08 Synthesis

## Question

Following the full chain (terrain and lifts to capacity, snow reliability
adjusting capacity, capacity to workforce, workforce to housing demand,
land capacity for supply), what does one scenario table and its
accompanying maps show?

## Decision

[left for the user]

## Alternatives considered

To be filled by `/step 08`.

## Evidence

No claims are tagged to this step yet. This step combines the outputs of
steps 03-07 rather than introducing new facts of its own, so its evidence
is those steps' data/processed outputs, not new claim IDs.

## Inputs

To be filled by `/step 08`.

## Method

To be filled by `/step 08`.

## Outputs

To be filled by `/step 08`.

## Checks

To be filled by `/step 08`.

## Open issues

- This step cannot be built until steps 03-07 each have at least a
  draft Method and a data/processed output, since it depends on all of
  them.
- The unresolved conflicts and gaps in steps 03-07 (the workforce
  headcount conflict in step 05 above all) propagate into this step's
  scenario table. Whether the synthesis carries both sides of an
  unresolved conflict as separate scenario rows, or waits until step 05
  resolves it, is a decision for the user once step 05 is further along.

## Status

draft

## Changelog

- 2026-09-10: created.
